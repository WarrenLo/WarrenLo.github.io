import React from 'react'

const NoRetract = (props) =>{
  return(
    <button className='control__btn control__rets--noretract' onClick={props.noretract}>No Ret.</button>
  )
}

export default NoRetract
